const calculator = {
  add(n1, n2) {
    return (n1 * 1) + (n2 * 1);
  },
  subtract(n1, n2) {
    return (n1 * 1) - (n2 * 1);
  },
  divide(n1, n2) {
    return (n1 * 1) / (n2 * 1);
  },
  multiply(n1, n2) {
    return (n1 * 1) * (n2 * 1);
  }
};

export default calculator;
